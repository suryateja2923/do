/**
 * Compatibility re-export: exposes apiClient as `api` to match legacy imports
 * from @admin/services/api across manager-web service files.
 */
export { apiClient as api } from '@/api/apiClient';
